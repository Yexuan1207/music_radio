export type PlayModeMapType = {
    sequence: {
        code: string,
        icon: string,
        name: string
    },
    loop: {
        code: string,
        icon: string,
        name: string
    },
    random: {
        code: string,
        icon: string,
        name: string
    }
}